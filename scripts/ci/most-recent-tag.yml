---
platform: linux

image: docker:///robdimsdale/wl-1.5

inputs:
- name: wl-master

run:
  path: sh
  args:
  - -c
  - |
    git -C wl-master tag --sort=version:refname | tail -1 > tagname && cat tagname
